## Learned User Preferences

## Learned Workspace Facts

- For `vue-tsc --noEmit`: ApexCharts `legend.markers` should match `ApexOptions` by using `size` (not `width` / `height` / `radius`). For `d3-selection`, rely on devDependency `@types/d3-selection` rather than an empty `declare module 'd3-selection'` in `globals.d.ts`.
